{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ComplianceReport",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "status",
    "system",
    "machineChecks",
    "agentReviews",
    "findings",
    "diffSummary"
  ],
  "properties": {
    "schemaVersion": {
      "type": "string",
      "const": "ui-design-system-compliance-report/v1"
    },
    "status": {
      "type": "string",
      "enum": ["review-required", "compliant", "repair-approved", "repair-complete"]
    },
    "system": {"$ref": "#/$defs/system"},
    "machineChecks": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "status"],
        "properties": {
          "name": {"type": "string", "minLength": 1},
          "status": {
            "type": "string",
            "enum": ["verified", "agent-judgment", "degraded-unverified", "user-approved-deviation"]
          }
        }
      }
    },
    "agentReviews": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "status"],
        "properties": {
          "name": {"type": "string", "minLength": 1},
          "status": {
            "type": "string",
            "enum": ["verified", "agent-judgment", "degraded-unverified", "user-approved-deviation"]
          }
        }
      }
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "severity",
          "ruleId",
          "evidence",
          "reason",
          "recommendation",
          "confidence",
          "verificationStatus"
        ],
        "properties": {
          "severity": {
            "type": "string",
            "enum": ["critical", "high", "medium", "low"]
          },
          "ruleId": {"type": "string", "minLength": 1},
          "evidence": {"type": "string", "minLength": 1},
          "reason": {"type": "string", "minLength": 1},
          "recommendation": {"type": "string", "minLength": 1},
          "confidence": {"type": "number", "minimum": 0, "maximum": 1},
          "verificationStatus": {
            "type": "string",
            "enum": [
              "verified",
              "agent-judgment",
              "degraded-unverified",
              "user-approved-deviation"
            ]
          }
        }
      }
    },
    "diffSummary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["before", "after", "modified"],
      "properties": {
        "before": {"type": "string", "minLength": 1},
        "after": {"type": ["string", "null"], "minLength": 1},
        "modified": {"type": "boolean"}
      }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {"status": {"const": "review-required"}},
        "required": ["status"]
      },
      "then": {
        "properties": {
          "diffSummary": {
            "properties": {"modified": {"const": false}}
          }
        }
      }
    }
  ],
  "$defs": {
    "system": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "name", "assetVersion"],
      "properties": {
        "id": {"type": "string", "minLength": 1},
        "name": {"type": "string", "minLength": 1},
        "assetVersion": {"type": "string", "minLength": 1}
      }
    }
  }
}
